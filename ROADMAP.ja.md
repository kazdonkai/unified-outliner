# Unified Outliner ロードマップ

Unified Outliner は、単一のMarkdownノート内で意味のまとまりを安全に構造編集することへ集中する。Obsidian本体の機能を重複させるのではなく、ブロックの再配置・閲覧・再編集を強化する機能を優先する。

## 現在のリリース（1.0.0）

**リリース日: 2026-09-25**

Unified Outliner 1.0 で、このプラグインの中核的な範囲、すなわち単一のMarkdownノート内での安全な構造編集がそろった。

- **構造操作コマンド**: 構造単位の移動・レベル変更コマンド（セクション全体の移動と、段落・callout・blockquote・fenced code block・tableにも及ぶ最小安全ブロック移動を含む）、見出し行だけの操作、削除・挿入コマンドを備える。
- **Outline Tree View**: 左右を選べるサイドバー配置、インラインリネーム、モバイルのタップ／長押し操作とタッチ端末用の専用ドラッグハンドル（UXP-01／UXP-02）に対応する。セクション、リスト項目、段落、単体のcallout・blockquote・fenced code block・tableはドラッグ＆ドロップで移動でき、段落と単体ブロックはセクションをまたいで移動できる。その際、必要な空行は自動で補われる。段落・fenced code block・table・ミラーは、設定で有効にすると独立した行として表示できる。セクションやリスト項目の下に、新しいコードブロックや表を挿入することもできる。fold stateはファイル単位で競合解決を伴って保存され、本文エディタの折りたたみとの同期も選べる。
- **拡張ブロック**: 画像のリスト項目の直後にOCR転記または引用キャプションが続く場合、これを **List + Callout** / **List + Quote** としてまとめ、1つの単位として移動・削除・編集できる。
- **Partial Edit Pane**: パンくずナビゲーション、Subtree Navigator、ポップアウトウィンドウに対応する。leafのリスト項目のマーカーフリー編集、親項目の直接の子の操作（追加・削除・並べ替え・インライン編集・インデント/アウトデント、一度に1段階まで）、単体のcallout/blockquoteと拡張ブロックの構造化エディタ、フェンス行を隠して言語を別途選ぶfenced code blockの編集、構造検証付きのRaw／Tableの2モードによるtableの編集を備える。未保存の変更がないときの自動再同期と、Apply時の競合保護も備える。
- **ブロックのコピー**: セクション、リスト部分木、単体のcallout・blockquote・fenced code block・table、段落について、**ブロックをコピー**・**下に複製**・**ブロックを貼り付け**（後・上・子として）を、ツリーまたはコマンドパレットから同じノート内で行える。コピーは行を挿入するだけであり、既存の行を書き換えることはない。
- **同じノート内のミラー**: 同じノート内の見出しやブロックへのObsidianの埋め込み（`![[#見出し]]`、`![[#^block-id]]`）を、読み取り専用のミラー行として表示する（参照先が見つからない場合と循環参照の場合は明示する）。行のクリックでは埋め込み行へ移動し、ダブルクリックと「参照先へ移動」メニューでは参照先へ移動する。モバイルでは、選択済みの行を再タップするたびに参照先と埋め込み行が切り替わる。ミラーは、ツリーまたはコマンドパレットから作成でき、ブロックIDの自動付与（`^uo-…`）と循環参照の拒否を伴う。作成したミラーはセクション内で移動でき、削除しても参照先のブロックは一切変わらない。Partial Edit Paneには、そのブロックを参照しているミラーの件数（「このブロックを参照しているミラー: N 件」）が表示され、クリックでミラーへ移動できる。ミラーの実体は埋め込み記法そのものだけであり、データベースは持たない。
- **設定**: 「アウトラインツリーの表示内容」「アウトラインツリーの見た目」「移動操作」「編集・操作」に分類した「全般」タブと、「拡張ブロック」タブからなる。UIは日本語と英語に対応する。

## 次の重点領域

- **ミラーの拡張**: ミラー行からの参照先の編集とその表示の同期、新しいミラーの作成位置の指定（貼り付けと同様の「ここにミラーを作成」の流れ）、ミラー行のドラッグ＆ドロップ、他のノートのブロックへのミラー、ミラーとコピーの視覚的な区別を検討する。
- **Move up/down のセクション越え対応**: 段落・単体ブロック・ミラーの **Move up/down** コマンドも、ドラッグ＆ドロップと同様にセクションの境界を越えられるようにする。
- **宣言的設定APIへの対応**: Obsidian 1.13.0 以降の `PluginSettingTab.getSettingDefinitions()` を `src/settings.ts` に実装し、現行 `display()` の各項目を移植する。Obsidian 1.12.x 以前向けに `display()` は残す（0.7.3 に対する Obsidian Community 審査の Warning 対応であり、まだ実装していない）。
- **tableとコードブロックの編集の拡充**: 現在のRaw／Tableの2モードと、fenced code blockの本文の生テキスト編集を超える機能、たとえばTableモードのセル内でのインラインMarkdownの描画や、列幅をそろえる整形を検討する。
- **任意の深さ・親への自由な移動**: 現在の1段階までのインデント/アウトデントを超えて、リスト項目や子を任意の祖先・任意の深さへ移動できるようにする方向性を検討する。
- **部分木単位での子操作の完全対応**: Partial Edit Paneの子の追加・削除・並べ替え・インデント/アウトデント対応を1段階を超えて拡張し、子の部分木全体をまとめて操作できるようにする。
- **Partial Edit Pane内でのドラッグ＆ドロップ**: 既存のボタン操作に加えて、ペインのプレビュー内で子を直接ドラッグ＆ドロップして並べ替えられるようにする方向性を検討する。
- **境界判定できない構造の安全な保護**: 追加・削除の境界が確定できないブロックや未対応の入れ子構造では、編集を拒否して原文を保護する方針を維持する。
- **ホイスト相当の編集の継続検証**: 選択したsection・list subtree・段落・単体callout/blockquote・fenced code block・table・親子構造を、そこだけに集中できる編集文脈として開く機能の安全性・回帰試験を継続する。

## その後の方向性

- ノード単位のリンク・埋め込みプレビュー。
- ノード内のリンクと添付ファイルの一覧。
- sectionまたはlist subtree単位でのCanvas連携。
- ステータスやタグなどの局所メタデータ。
- ノート横断のブロック分類・検索（Phase 6: BlockIndexによるYAML継承・inline property統合）。
- 構造図とダイアログによる編集（Phase 7）。

## 設計原則

### Callout / Blockquote の行継続に関する安全境界

Unified Outliner は、callout および blockquote の編集可能範囲を、
Markdown 上で quote prefix `>` が明示された連続行だけに限定する。

Obsidian のレンダリング上、callout header または quote 行に空行なしで続く
prefixなしの通常テキストが、視覚的に callout / blockquote の内部に表示される場合がある。
しかし Unified Outliner は、その表示上の継続を callout / blockquote の構文的な一部として
推測・補完・再直列化しない。

したがって、prefixなし継続行は以下のように扱う。

- callout / blockquote の編集対象範囲には含めない
- Partial Edit Pane には読み込まない
- Apply 時に `> ` prefix を自動付与しない
- Tree 上の callout / blockquote member として投影しない
- CompositeBlock の member range に含めない
- 通常の paragraph または既存 parser が決定する別の block として扱う

対象 range が prefixなし継続行を含むため安全に一意決定できない場合は、
Partial Edit を拒否し、本文を変更しない。

## 意図的に対象外とするもの

Unified Outlinerは、汎用全文検索、タスク管理、Dataview型の集計、AIによる書き換えを置き換えることを目指さない。Markdownノートの信頼できる構造編集に集中する。
